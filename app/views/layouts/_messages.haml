- flash.each do |name, msg|
  - if msg.is_a?(String)
    %div{:class => "alert alert-#{['success', 'notice'].include?(name.to_s) ? 'success' : 'danger'}"}
      %button.close{"aria-hidden" => "true", "data-dismiss" => "alert", :type => "button"} ×
      = content_tag :div, msg, :id => "flash_#{name}"
